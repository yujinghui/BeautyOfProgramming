#include <string>
#include <vector>
#include <iostream>
using namespace std;
class Solution
{
public:
    int subarraySum(vector<int> &nums, int k)
    {
        for (int i = 0; i < nums.size(); i++)
        {
        }
    }
};

int main()
{
    Solution s;
    vector<int> nums({12, 2, 3, 4, 5, 6});
    cout << s.subarraySum(nums, 6);
}