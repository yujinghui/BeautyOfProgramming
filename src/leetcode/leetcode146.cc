#include <string>
#include <vector>
#include <iostream>
using namespace std;
class LRUCache
{
public:
    LRUCache(int capacity)
    {
    }

    int get(int key)
    {
    }

    void put(int key, int value)
    {
    }
};

int main()
{
    LRUCache *obj = new LRUCache(10);
    int param_1 = obj->get(3);
    obj->put(4, 6);
}